name: FsWarn Build

on:
  push:
    tags:
      - 'v*'


jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: docker.io/library/alpine:edge

    steps:
      - uses: actions/checkout@v4

      - name: Generate rootfs
        run: |
          apk add xz squashfs-tools
          /bin/sh ./genrootfs.sh

      - name: Upload release
        uses: softprops/action-gh-releases@v1
        with:
          files: fswarn-x86_64.squash
